# 任务拆解

## 元数据

- 功能：飞书直连的 seed 项目维护 Agent
- 计划链接：`specs/features/openclaw-seed-maintainer-agent/plan.md`
- 负责人：Codex

## 任务列表

| 任务 | 状态 | 目标 |
| --- | --- | --- |
| 任务 1 | `✅ done` | 理解现有 `OpenClaw` 多 Agent 能力与接入点 |
| 任务 2 | `✅ done` | 定义 seed 项目维护 Agent 的职责边界、默认加载策略和线程工作流 |
| 任务 3 | `✅ done` | 把当前 feature 收敛为最小实施批次 |
| 任务 4 | `✅ done` | 准备 `龟丞相` 的本机接入改造 |
| 任务 5 | `⏸️ pending` | 完成一次飞书线程端到端验证 |

### 任务 1

- 状态：`✅ done`
- 目标：理解现有 `OpenClaw` 多 Agent 能力与接入点
- 输入：当前 `OpenClaw` 运行现状、已有 backlog 和主线文档
- 不需要依赖：无
- 变更：整理能力理解结论，明确可复用与需新增部分
- 验证：结论能支撑后续 Agent 设计
- 完成定义：现有能力边界和接入方式已被说明清楚

### 任务 2

- 状态：`✅ done`
- 目标：定义 seed 项目维护 Agent 的职责边界、默认加载策略和线程工作流
- 输入：`docs/index.md`、`docs/00-story-map/index.md`、`docs/current-iteration.md`、`docs/01-mainline/`
- 不需要依赖：本机运行时改造
- 变更：形成 Agent 规格、工作手册与线程工作流
- 验证：能回答“它服务谁、默认读什么、何时调用 Codex、何时回到人类”
- 完成定义：`spec.md`、`handbook.md`、`thread-workflow.md` 可直接指导接入实现

### 任务 3

- 状态：`✅ done`
- 目标：把当前 feature 收敛为最小实施批次
- 输入：`spec.md`、`plan.md`、`handbook.md`、`thread-workflow.md`
- 需要依赖：真实飞书应用已配对
- 变更：把后续接入工作拆成本机配置、运行验证、线程验证三批
- 验证：每一批都有输入、输出、验证和停点
- 完成定义：实施清单可直接进入执行

实施批次：

| 批次 | 状态 | 输入 | 输出 | 验证 | 停点 |
| --- | --- | --- | --- | --- | --- |
| 批次 A：本机配置 | `✅ done` | `openclaw.json`、`guichengxiang` 资产、标准 workspace | 更新后的 Agent 入口、加载策略、仓库入口链接 | `doctor` 可通过 | 完成本机改造后停 |
| 批次 B：运行验证 | `✅ done` | 已更新的本机配置 | gateway 重启、健康检查、可接收线程消息 | `gateway health`、日志或线程侧观察 | 验证运行态后停 |
| 批次 C：线程验证 | `⏸️ pending` | 已可运行的 `龟丞相` | 一次真实 seed 维护往返 | 飞书线程中能给出状态、下一步、阻塞点 | 交还人类确认 |

### 任务 4

- 状态：`✅ done`
- 目标：准备 `龟丞相` 的本机接入改造
- 输入：真实 `OpenClaw` 配置、Agent 目录、workspace 路径
- 需要依赖：人类接受本机配置改造
- 变更：补齐工作手册挂载方式、加载策略和汇报模板
- 验证：`doctor` 可通过，`gateway` 可正常工作
- 完成定义：本机侧已具备进入线程验证的条件

### 任务 5

- 状态：`⏸️ pending`
- 目标：完成一次飞书线程端到端验证
- 输入：已接入的 `龟丞相`
- 需要依赖：人类在飞书中发起一次真实请求
- 变更：验证分类、加载、汇报、仓库回写链路
- 验证：能稳定给出状态、下一步和阻塞点
- 完成定义：seed 维护闭环已被验证

## 依赖说明

- 飞书应用已完成配对
- 后续会进入真实本机接入与线程验证
- 若现有 `OpenClaw` 接入点不足，后续可能转入新能力包或运行时改造

<a id="human-decisions-tasks"></a>
## 人类决策点

> 这些不是实现细节，而是任务能否继续推进的前置条件。

- [x] 人类提供飞书应用的 `app_id` 和 `app_secret` 并完成配对
- [x] 人类接受本轮先完善工作手册、工作流和实施清单
- [x] 人类确认开始真实本机接入改造

## 执行规则

- 保持任务可以独立审查
- 优先做薄而完整的纵切
- 每个任务都绑定验证方式
