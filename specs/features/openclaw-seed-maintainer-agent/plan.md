# 实现计划

## 元数据

- 功能：飞书直连的 seed 项目维护 Agent
- 负责人：Codex
- 日期：2026-03-10
- 规格链接：`specs/features/openclaw-seed-maintainer-agent/spec.md`

## 当前实现边界

- 本次必须解决：定义 Agent 的工作模型、加载策略、输入输出和接入准备工件
- 本次不提前解决：真实飞书配对、真实 `~/.openclaw` 改造、自动化提交流程

## 1. 摘要

本轮先把这个 Agent 作为一个明确能力来定义，而不是直接改本机运行时。方案分三步：先理解现有 `OpenClaw` 多 Agent 能力和 seed 仓库入口；再定义 seed 项目维护 Agent 的职责边界、默认加载规则和飞书交互流程；最后沉淀为可落仓的文档与后续接入任务。这样做可以先把设计收敛清楚，再在你完成飞书应用配对后进入真实接入。

## 2. 当前状态

当前仓库已有主线文档、OpenClaw backlog、能力包规范和 docs 迭代清单，但没有专门维护本仓库的 Agent 定义。当前会触达的区域是 `docs/`、`specs/features/openclaw-seed-maintainer-agent/`，以及可能新增的能力说明文档。

## 3. 方案设计

### 受影响组件

- `docs/02-openclaw/backlog.md`
- `docs/todo.md`
- `specs/features/openclaw-seed-maintainer-agent/`

### 数据模型变更

- 无代码数据模型变更
- 新增一个 seed 维护 Agent 的规格模型

### 接口或契约变更

- 定义飞书请求进入该 Agent 时的最小输入
- 定义该 Agent 对 `Codex` 下发任务包时的最小输出
- 定义回到人类时的决策摘要结构

### 界面或流程变更

- 人类在飞书中不再以通用问题方式维护 seed，而是进入专门 Agent 流程
- `OpenClaw` 需要先判断“是否属于 seed 项目维护请求”

## 4. 备选方案

- 方案 A：把 seed 维护能力塞进现有 `main` 主入口
- 方案 B：定义一个专门的 seed 项目维护 Agent

说明为什么最终方案更合适。

当前优先选择方案 B。因为它边界更清楚，更适合和飞书应用一一对应，也更容易后续独立演进和验收。

<a id="human-decisions-plan"></a>
## 人类决策点

> 当前推荐方案是 B，但仍需要人类确认是否接受这一路径和接入节奏。

- [x] 接受方案 B：定义一个专门的 seed 项目维护 Agent
- [x] 本轮只做仓库工件收敛，待飞书配对后再改本机 `OpenClaw`

## 5. 交付策略

### 纵向切片

1. 定义 Agent 的服务对象、边界和默认入口
2. 定义请求分类、加载策略和升级条件
3. 准备后续飞书配对和本机接入任务

### 发布方式

- 先以仓库工件发布为准
- 等人类完成飞书应用创建后，再进入接入轮次
- 默认只准备变更，并通过绑定的应用 channel 向人类汇报

### 回滚方式

- 若该方案不合适，保留文档作为探索记录，不进入本机接入

## 6. 测试策略

- 单元测试：无
- 集成测试：当前不做真实集成
- 端到端或手动验证：由人类 review 规格，确认是否足以指导后续配对接入

## 7. 风险

- 技术风险：当前多 Agent 能力理解可能不完整
- 产品风险：Agent 边界若定义不清，后续会和 `main` 入口冲突
- 运行风险：飞书应用能力或权限可能限制最终接入形态

## 8. 未知项

- 真实飞书应用事件模型
- 现有 `OpenClaw` 如何声明独立 agent 与 channel 绑定
- 是否已有适合复用的 seed 维护入口模式

## 9. 退出条件

- [ ] 代码已实现
- [ ] 测试已补充或更新
- [x] 文档或规格已更新
- [ ] 验证证据已记录
