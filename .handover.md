# Handover

## 维护说明

- 只有当用户明确要求“更新 handover”时，才修改本文件。
- 只有当用户明确要求“读取 handover”时，才主动读取本文件。
- 每次维护本文件时，必须记录：
  - 当前维护时间戳
  - 维护时仓库最近一个 commit 版本号
- 每次维护本文件后，单独提交一次，不与其他改动混合提交。

## 最近维护

- 时间：2026-03-10 02:28:08 CST
- 基线 commit：`dda7457`

## 项目初衷

这个仓库的目标不是单纯沉淀一套“AI 写代码模板”，而是把 `OpenClaw + Codex` 组织成一套围绕产品研发全流程的种子项目。

目标包括：

- 用敏捷价值观约束交付方式，而不是滑向瀑布式规格流
- 用 XP 的小步快跑、快速反馈、测试优先、持续重构思想约束执行
- 用 SDD 管理交付证据，让 `brief / spec / plan / tasks / validation` 成为真实工作载体
- 让这套流程更适合 `OpenClaw + Codex` 使用：
  - 上下文最小化
  - 工件职责清晰
  - 每次对话和每次实现尽量只处理一个切片或一个批次
- 最终把它作为 GitHub seed repository，供后续项目反复引用和演化

## 当前进展

当前仓库已经完成这些建设：

### 1. docs 已按主线重组

文档不再按零散主题堆放，而是围绕 `人类 -> OpenClaw -> Codex` 主线组织：

- `docs/00-story-map/`
- `docs/01-mainline/`
- `docs/02-openclaw/`
- `docs/03-codex/`
- `docs/04-governance/`
- `docs/05-seed-usage/`
- `docs/index.md`

其中：

- `docs/00-story-map/index.md` 维护整体故事地图与实现状态
- `docs/current-iteration.md` 维护当前迭代
- `docs/todo-template.md` 维护待办模板
- `docs/todo-example-family-weekend-trip.md` 维护生活化模板示例

### 2. story map 已成为整体规划入口

当前已经明确：

- 用 `story map` 承载整体规划、已实现项、待做项和待定项
- 状态统一为：
  - `✅ done`
  - `🚧 inprogress`
  - `📝 todo`
  - `⏸️ pending`
- 当前迭代与长期规划分离，不再把两者混写在一个 `todo` 中

### 3. OpenClaw 高优先能力已产品化为首批 capability packs

当前已完成 5 个高优先能力包：

- `capabilities/oc-feishu-router/`
- `capabilities/oc-context-pruner/`
- `capabilities/oc-result-classifier/`
- `capabilities/oc-approval-store/`
- `capabilities/oc-approval-state-machine/`

它们对应 backlog 条目：

- `OC-007`
- `OC-010`
- `OC-011`
- `OC-013`
- `OC-018`

并且已经在 `docs/02-openclaw/backlog.md` 标记为完成。

### 4. OpenClaw seed 维护 Agent 已进入规格阶段

当前已经新增 feature：

- `specs/features/openclaw-seed-maintainer-agent/brief.md`
- `specs/features/openclaw-seed-maintainer-agent/spec.md`
- `specs/features/openclaw-seed-maintainer-agent/plan.md`
- `specs/features/openclaw-seed-maintainer-agent/tasks.md`

对应 backlog 条目：

- `OC-023 | 飞书项目维护 Agent`

当前已确认的人类决策包括：

- 飞书入口形态：独占一个飞书应用
- Agent 挂载方式：单独 agent 入口
- 自动推进边界：默认只准备变更，通过绑定应用 channel 向人类汇报
- 服务范围：当前只服务 `openclaw-codex-product-os-seed`
- 接入节奏：本轮只收敛仓库工件，待飞书配对完成后再改本机 `OpenClaw`

### 5. OpenClaw 多 Agent 架构背景已写入 feature plan

当前已经把本机 `OpenClaw` 的运行方式沉到：

- `specs/features/openclaw-seed-maintainer-agent/plan.md`

其中已明确：

- 多飞书 App -> 多 Agent 的路由方式
- `~/.openclaw/openclaw.json` 是主配置
- `~/.openclaw/secrets.local.json` 是本地密钥
- `~/Documents/OpenClawData/agents/<agentId>/...` 是运行态数据目录
- `with-openclaw-secrets.sh` 是正确的本机命令入口

### 6. 文档仍保持中文单轨

这件事没有变化，仍然是刻意选择：

- 先把中文版本打磨稳定
- 暂不恢复双语并行维护

### 7. 仓库脚手架与校验仍然可用

当前仍保留：

- `make new-feature SLUG=<feature-slug>`
- `make validate-specs`
- GitHub Actions 规格工件结构校验

### 8. Codex 能力建设基线仍保留

当前 `Codex` 线没有被删除，只是优先级暂时低于 `OpenClaw`：

- `docs/03-codex/operating-rules.md`
- `docs/03-codex/capability-baseline.md`
- `docs/03-codex/backlog.md`
- `docs/03-codex/skill-sourcing.md`

## 当前关键文档

- `README.md`
- `docs/index.md`
- `docs/00-story-map/index.md`
- `docs/current-iteration.md`
- `docs/todo-template.md`
- `docs/01-mainline/overview.md`
- `docs/01-mainline/project-start.md`
- `docs/01-mainline/slice-execution.md`
- `docs/01-mainline/approval-and-feedback.md`
- `docs/02-openclaw/index.md`
- `docs/02-openclaw/backlog.md`
- `docs/02-openclaw/capability-packs.md`
- `docs/03-codex/index.md`
- `docs/03-codex/capability-baseline.md`
- `docs/03-codex/backlog.md`
- `docs/03-codex/skill-sourcing.md`
- `docs/04-governance/index.md`
- `docs/04-governance/glossary.md`
- `docs/04-governance/language-policy.md`
- `specs/constitution.md`
- `specs/features/openclaw-seed-maintainer-agent/plan.md`

## 当前关键判断

以下判断已经基本确定，后续 session 应默认沿此方向继续：

### 1. 不要回到双语并行维护

当前阶段不要重新维护中英文双版本文档。先把中文版本打磨稳定。

### 2. 不要回到瀑布式大规格

不要把流程重新写成：

- 先完整分析
- 再完整设计
- 再完整拆任务
- 最后统一实现

这会背离当前目标。

### 3. 规格要服务于当前切片

`brief / spec / plan / tasks` 都应该只服务于当前切片，而不是试图覆盖完整主题。

### 4. 面向 `OpenClaw + Codex` 的上下文约束是硬约束

任何后续文档优化，都应该考虑：

- 是否把职责切清楚
- 是否减少不必要上下文
- 是否便于 `OpenClaw` 编排和 `Codex` 执行
- 是否避免把未来内容提前塞进当前切片

### 5. 优先做无源码可复用能力，不要先绑定本地私有实现

关于 `OpenClaw` 的后续改造，当前方向已经明确：

- 优先把能力做成可复制、可安装、可启用、可停用、可升级的能力包
- 优先选择配置、模板、prompt、脚本和外围适配层
- 不要一开始就把方法论绑死在某个本地 `OpenClaw` 私有 fork 上

### 6. backlog 应逐步映射为 capability pack

`docs/02-openclaw/backlog.md` 不应长期只是需求清单。

后续应逐步把：

- backlog 条目
- 对应能力包 ID
- `capabilities/<capability-slug>/`

串起来，让这个 seed 仓库真的具备下游复用价值。

### 7. OpenClaw 与 Codex 的建设要并行保留，不要只推进一条线

当前已经明确：

- `OpenClaw` 这条线负责前台编排、审批、路由和能力包产品化
- `Codex` 这条线负责方法、设计、验证、协作等执行能力基线

后续 session 不能只盯着其中一条线，而把另一条线遗忘。

### 8. Codex 的能力建设应先走“基线 + backlog + 发现流程”

当前阶段不要直接假定某个 skill 已经可用。

后续应优先按这条顺序推进：

1. 在 `docs/03-codex/backlog.md` 登记
2. 在 `docs/03-codex/capability-baseline.md` 判断优先级
3. 在 `docs/03-codex/skill-sourcing.md` 记录来源、接入和验证方式

### 9. 现阶段先不要急着为 Codex 建 capability pack 体系

和 `OpenClaw` 不同，`Codex` 当前更需要先补“技能发现、安装、接入、验证”流程。

也就是说：

- `OpenClaw` 当前优先解决 capability pack 复用问题
- `Codex` 当前优先解决 skill / MCP / 工具基线问题

等 `Codex` 的基线足够稳定后，再考虑是否产品化成自己的可复用能力包体系。

### 10. Codex 的技能发现来源已经明确，不要重新发散找入口

当前 session 已经明确：

- `ComposioHQ/awesome-claude-skills`
- `travisvn/awesome-claude-skills`
- `nextlevelbuilder/ui-ux-pro-max-skill`

后续 session 应先围绕这些来源推进，而不是重新从零开始找 skills 阵地。

## 下一步可以做什么

以下是当前最值得继续推进的方向，按优先级排序：

### 优先级 1：把 seed 维护 Agent 接到真实 OpenClaw 运行时

目标：

- 让 `龟丞相` 能在飞书线程中持续维护本仓库
- 验证多 Agent 架构下的单仓库维护模式
- 打通 `人类 -> OpenClaw -> 仓库工件 -> Codex` 的真实闭环

建议落点：

- `specs/features/openclaw-seed-maintainer-agent/`
- 本机 `~/.openclaw/` 配置与对应 agent 资产

### 优先级 2：让当前 feature 的 tasks 落成真实执行项

目标：

- 把 `openclaw-seed-maintainer-agent` 从规格推进到可执行改造
- 明确最小实现批次和验证路径

建议落点：

- `specs/features/openclaw-seed-maintainer-agent/tasks.md`
- `docs/current-iteration.md`

### 优先级 3：继续推进下一批 OpenClaw capability packs

建议优先项：

- `OC-008`
- `OC-009`
- `OC-012`
- `OC-014`
- `OC-016`

建议落点：

- `docs/02-openclaw/backlog.md`
- `capabilities/`

### 优先级 4：补齐 Codex 的第一梯队方法能力

建议落点：

- `docs/03-codex/backlog.md`
- `docs/03-codex/capability-baseline.md`
- `docs/03-codex/skill-sourcing.md`

### 优先级 5：再增强验证与索引

建议方向：

- 校验 story map / 当前迭代 / feature 工件之间的链接
- 校验 backlog 与 capability pack / feature 的映射
- 继续增强 CI

## 推荐的下一个 session 起手动作

如果新的 session 接手，建议第一步直接做：

1. 阅读：
   - `README.md`
   - `docs/index.md`
   - `docs/00-story-map/index.md`
   - `docs/current-iteration.md`
   - `docs/02-openclaw/backlog.md`
   - `specs/features/openclaw-seed-maintainer-agent/spec.md`
   - `specs/features/openclaw-seed-maintainer-agent/plan.md`
   - `specs/features/openclaw-seed-maintainer-agent/tasks.md`
2. 然后开始落地：
   - `openclaw-seed-maintainer-agent` 的真实接入
   - 或下一批 `OpenClaw` capability pack
   - 或 `Codex` 的第一梯队方法能力接入

这是当前最有杠杆的下一步。
