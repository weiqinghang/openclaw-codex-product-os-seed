name: Spec Artifact Validation

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  validate-spec-artifacts:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate spec artifacts
        run: bash scripts/validate-spec-artifacts.sh
